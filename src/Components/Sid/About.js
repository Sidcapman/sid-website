import React from 'react'

function About() {
  return (
    <div
      id="#about"
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '90vh',
      }}>
      About
    </div>
  )
}

export default About
